#!/bin/sh
# Pre-commit hook runner for include guard and code formatting verification.
set -e

REPO_ROOT=$(git rev-parse --show-toplevel)
cd "$REPO_ROOT" || exit 1

python3 ci/check_include_guards.py
python3 ci/format_code.py
